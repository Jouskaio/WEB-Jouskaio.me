```jsx
<Code language={"python"} classname={"a-code--special"}>
  {
    `
    # For the exemple, the special theme doesn't not work because it is not download in styleguidist but in _document.tsx page
    def main():
    print("Hello World!")

    if __name__ == "__main__":
      main()
    `
  }
</Code>
```