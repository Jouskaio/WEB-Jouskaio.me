import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Welcome" />

# Welcome to Jouskaio.me Design System

This Storybook showcases the components used in the portfolio of **Jouskaio**.

## Project Overview

- **Framework:** Next.js
- **Styling:** SCSS (Atomic Design)
- **Data:** Apollo Client / GraphQL
- **Animations:** AOS (Animate On Scroll)
- **Code Highlighting:** highlight.js

## Installation

To install the library in your project, run:

```bash
yarn add @jouskaio/ui
# or
npm install @jouskaio/ui
```

### Usage

Don't forget to import the styles in your application (e.g., in `_app.tsx` or `layout.tsx`):

```tsx
import '@jouskaio/ui/styles';
```

Then you can use the components:

```tsx
import { TextH1 } from '@jouskaio/ui';

const MyPage = () => (
  <TextH1>Hello World</TextH1>
);
```

## Development

To start Storybook locally:

```bash
yarn storybook
```

To run tests:

```bash
yarn test-storybook
```
